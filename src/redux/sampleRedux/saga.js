import { all } from 'redux-saga/effects'
// import { get, post } from '@iso/helpers/resApiRequestor';
// import { setSession, getSession, setSessionTheme } from '@iso/helpers/session';
// import actions from './actions';

export default function* rootSaga() {
  yield all([
  ])
}